# Copy to .env and edit before `docker compose up -d`.
# The token is the dashboard login password AND the value the APK sends as
# ?token= — pick something long and random.
DASH_TOKEN=change-me-to-a-long-random-string
DASH_PORT=8765